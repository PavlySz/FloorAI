keys.env
.env
**/**__pycache__/
*.pyc
venv/
.venv/
.claude/
.history/
floorai/

# generated at runtime
scenes/
static/renders/
server.log

# intermediate scrape output; catalog/*.json is the committed result
scripts/*.json

# leading slash anchors to the repo root; "*/api_outputs/" only matched one
# level deep and so never matched the top-level folder
/api_outputs/
